# 배경 이미지 (project2)

Design > Advanced > **Background Image** 에서 페이지 배경 이미지 URL을 설정할 수 있습니다.

- **로컬 파일**: 이 폴더에 이미지를 넣고 경로를 `/파일명.png` 로 입력 (예: `/bg-project2.png`)
- **외부 URL**: 전체 URL 입력 가능

project2 기본값은 `/bg-project2.png` 이므로, 원하는 이미지를 `bg-project2.png` 로 저장해 두면 바로 적용됩니다.
